{
    "name": "ecommerce-app",
    "version": "1.0.0",
    "description": "Ecommerce app built with typescript and react",
    "main": "index.js",
    "scripts": {
        "build": "cd backend && npm install && npm run build  && cd ../frontend && npm install && npm run build",
        "start": "ts-node backend/src/index.ts",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "repository": {
        "type": "git",
        "url": ""
    },
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "@types/express": "^4.17.17"
    }
}
